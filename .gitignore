node_modules
.next
out
build
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store
Thumbs.db

# Local tooling/config — not part of the app
.claude/
strip_claude.*

# Local-only AI agent continuity notes — see memory/README.md. Never committed;
# regenerate/update per-machine, not shared via PRs.
memory/

# Personal blog-drafting notes — not part of the public project
docs/PRODUCT_LOG.md
